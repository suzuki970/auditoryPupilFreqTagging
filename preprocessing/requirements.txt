# Raw-data preprocessing (.asc -> data/<date>/s*_trial.json).
# Tested with CPython 3.10.18 (conda env "denso_py310").
#
#   conda create -n pft python=3.10
#   conda activate pft
#   pip install -r requirements.txt
#
# Also requires the PupilAnalysisToolbox lib on PYTHONPATH -- see
# ../README.md ("Preprocessing" section) for the five external modules
# (au2mm, band_pass_filter, pixel_size, pre_processing, zeroInterp) this
# code imports but does not vendor.

numpy==2.2.6
pandas==2.3.1
scipy==1.15.3
matplotlib==3.10.5
seaborn==0.13.2
tqdm==4.67.1
PyWavelets==1.8.0
statsmodels==0.14.6
